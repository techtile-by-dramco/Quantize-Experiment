Energy-ball run using transmission with the best phase.

Phase perturbation used a von Mises distribution:
kappa = 1.0 / (delta_phi ** 2)
applied_delta = np.random.vonmises(mu=0.0, kappa=kappa)

delta_phi = pi/4 for this run.
